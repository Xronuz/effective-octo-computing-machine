# XAVFSIZ XONADON — Python bog'liqliklari
# Python 3.11, FastAPI 0.110+

# ============ Web framework ============
fastapi>=0.110.0,<0.111.0
uvicorn[standard]>=0.25.0

# ============ Ma'lumotlar bazasi ============
sqlalchemy[asyncio]>=2.0.25,<2.1.0
asyncpg>=0.29.0
alembic>=1.13.0
geoalchemy2>=0.14.0

# ============ Xavfsizlik ============
python-jose[cryptography]>=3.3.0
bcrypt>=4.0.0
python-multipart>=0.0.6

# ============ Validatsiya ============
pydantic>=2.5.0,<3.0.0
pydantic-settings>=2.1.0

# ============ Rasmga ishlov berish ============
Pillow>=10.1.0

# ============ Rejalashtiruvchi ============
APScheduler>=3.10.0

# ============ Telegram bot ============
aiogram>=3.3.0

# ============ HTTP (Expo push uchun) ============
httpx>=0.25.0

# ============ Eksport ============
openpyxl>=3.1.0
reportlab>=4.0.0

# ============ Rate limiting ============
slowapi>=0.1.9

# ============ Monitoring / util ============
python-dotenv>=1.0.0
